import React from 'react';

const person = (props) => {

    // <div>
     return  <p>I'm a {props.name} and I am {props.age} years old!</p>
      // <input type="text" onChange = {} value = {props.name}/>
    // </div>

};

export default person;
